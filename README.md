
  # Proposal Layout Viewer

  This is a code bundle for Proposal Layout Viewer. The original project is available at https://www.figma.com/design/vNKufVms250kCPC40AelpW/Proposal-Layout-Viewer.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  